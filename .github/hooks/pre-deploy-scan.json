{
  "$schema": "https://raw.githubusercontent.com/github/copilot-hooks/main/schema.json",
  "name": "pre-deploy-scan",
  "description": "Run Salesforce Code Analyzer before deployments",
  "event": "PreToolUse",
  "tools": ["run_in_terminal"],
  "match": {
    "command": "sf project deploy"
  },
  "action": {
    "type": "prompt",
    "message": "⚠️ Running security scan before deployment...",
    "command": "sf code-analyzer run --workspace force-app --rule-selector Security --severity-threshold 2 --view table"
  },
  "onFailure": {
    "type": "block",
    "message": "❌ Deployment blocked: Critical security issues found. Fix the issues above before deploying."
  }
}
